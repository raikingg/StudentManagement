schema{
    query:Query
}
type Query{
    addStudent: Student
    findAll: [Student]
    findById(studentId:String): Student
}
type Student{
    studentId:String,
    studentName:String,
    city:String,
    marks:String
}